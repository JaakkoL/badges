[project]
name = "badges"
version = "0.1.0"
description = "Playing with code coverage badges in Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.0",
    "pytest-cov>=6.1.1",
]
